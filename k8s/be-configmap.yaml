# 설정/비밀번호 주입 > 환경 변수 (비밀 아닌 것들)
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: app
data:
  SPRING_PROFILES_ACTIVE: "dev"
  DB_HOST: "mariadb-svc"
  DB_PORT: "3306"
  REDIS_HOST: "redis-svc"
  REDIS_PORT: "6379"
