# 프론트/Ingress가 붙는 대상
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8888
      targetPort: 8888
